# 2026-07-09

## 修复审批保存失败 Bug

**问题**: James 点击审批通过后，售后记录没有审批成功。

**根因**: `saveRecordAsync` 函数在发送 PUT 请求后从未检查 HTTP 响应状态码。服务器返回 403/500 等错误时，代码静默忽略，前端以为保存成功但实际上没有。

**修复**:
1. `saveRecordAsync` (行 1562): 添加 `resp.ok` 检查，非成功响应抛出 Error
2. `executeApproval` (行 4363): 改为只保存被修改的记录（原来保存全量），添加 try/catch 处理保存错误，失败时显示"保存失败"提示

## 修复审批响应慢 + 页面不刷新问题

**问题**: 点击"审批通过"后延迟数秒才弹"审批成功"提示，且审批完成后页面仍显示审批按钮可重复操作。

**根因**:
1. `executeApproval` 中 `await sendApprovalNotification()` 阻塞主流程（飞书 API 每次调用超时 10s）
2. 记录提交时也阻塞在 `await sendApprovalNotification()`
3. 审批成功后未清除前端缓存，导致重新渲染时可能读取过期缓存
4. 无防重复提交机制

**修复 (index.html)**:
1. `sendApprovalNotification` 改为 fire-and-forget 非阻塞模式（`.catch()` 静默处理错误）
2. `doApproval` 添加按钮禁用/恢复逻辑防止重复提交
3. 审批成功后强制清除缓存（`_recordsCache=null; _cacheTimestamp=0`）
4. `await renderDetail()` 确保详情页从服务器拉取最新数据
5. 记录提交时（行 4185）同样改为非阻塞通知

**验证**: API 测试确认审批后数据库正确更新（status=已完成, level=0），`canApproveRecord` 检测 `current_approval_level<=0` 阻止已完成记录的重复审批

## 添加调试日志

在 4 处关键位置加了 console.log：
- `saveRecordAsync` (index.html 1574): 记录 PUT 请求的 id/status/level 和响应状态
- `executeApproval` (index.html 4375): 记录保存前后的审批记录状态
- `server.js` PUT handler (873): 记录收到的 status/level/hLen 和 userId
- `server.js` PUT handler (940): 记录 UPDATE 返回的 status/level

## 修复列表"审批状态"列显示为空

**问题**: 售后记录列表中"审批状态"这一列显示为空（同行业务"处理状态"正常显示 ERP入库）。

**用户要求（直接执行，不再发散查根因）**:
1. 在列表渲染审批状态列处临时加 `console.log('row status:', r.id, r.status, r.approval_status, r.approvalStatus)`
2. 审批状态列加兜底：先显示原始值不走翻译 → `r.status || r.approval_status || r.approvalStatus || '待审批'`
3. 字段来源分离：审批状态只读售后单审批字段，处理状态只读明细处理字段（psCell=getRecordProcessSummary(items)），两者不混用

**修改 (index.html)**:
- 明细模式循环 (2903) 和按单模式循环 (2935)：均加 console.log
- 两处审批状态 `<td>` 改为 `<span class="status-badge">${r.status || r.approval_status || r.approvalStatus || '待审批'}</span>`（去掉 `status-${r.status}` class 与 `tApprovalStatus` 翻译）

**验证**: 同源测试页 fetch 真实 API + 新逻辑渲染 → `id=4 审批状态="已完成" | 处理状态="ERP入库(4)"`，列已正确显示且分离。

**说明**: 重点是先让页面把原始审批状态显示出来（走翻译函数可能在 r.status 为空/非映射值时返回空串导致空白）。待用户确认控制台 `row status` 日志后，再优化翻译与样式。

**临时调试文件已清理**: prefill.html / test-render.html / test-list.html / scripts/_probe_status*.js

## 排查"改动不生效" — 发现访问实例错位

**用户现象**: 列表审批状态列仍是空白，即便代码已改为兜底 `r.status || ... || '待审批'`。

**用户要求（不再发散，硬验证）**:
1. 确认页面加载的是哪个文件（index.html? dist/build? 多文件?）
2. 页面顶部加明显标记 "DEBUG-审批状态修复已加载"
3. 审批状态列临时写死 `<td>TEST审批状态</td>`
4. 看到 TEST 后再改回兜底逻辑
5. 报告：实际修改路径 / 当前加载路径 / 是否重复页面

**排查结论**:
- 项目只有 1 个 `index.html`，无 dist/build 副本，无第二个列表组件
- 列表有两种渲染模式（按明细 2913 / 按售后单 2951），两处审批列均已改
- 详情页(3253)、审批弹窗(4454)另有独立审批状态渲染（用 tApprovalStatus，非列表列）
- **铁证**: `curl http://localhost:3000/` 返回的 index.html 已含 DEBUG 横幅和 TEST 审批列 → 本地 localhost:3000 实例改动 100% 生效
- **关键发现**: 系统部署在 Render 生产环境 `https://aftersales-app.onrender.com`（见 render.yaml），那是独立实例，本地改 index.html 不会同步到生产；数据库是 Render 连接的 Supabase（同一库）

**当前临时状态**: index.html 顶部保留红色 DEBUG 横幅，两处审批列写死 `TEST审批状态`，供用户硬刷新 localhost:3000 验证。待用户确认看到横幅/列后，再改回 `|| '待审批'` 兜底并删横幅。

**待用户确认**: 浏览器地址栏是 localhost:3000 还是 aftersales-app.onrender.com？若连的是 Render，本地改动无效，需 git push + Render 重新部署。

## 线上修复已部署 (v3.9.14)

**确认用户访问的是 Render 生产环境** `https://aftersales-app.onrender.com`（非 localhost），故本地改动不生效。

**执行的生产修复**:
1. 删除 DEBUG 横幅 (原 514 行)
2. 删除两处 `TEST审批状态` 写死列，改回正式兜底 `r.status || r.approval_status || r.approvalStatus || 'Pending Approval'`（2908、2945）
3. 删除全部本次会话调试 console.log：index.html 1577/1583(saveRecordAsync)、2904/2936(row status)、4377/4380(DEBUG 保存)；server.js 880/939(PUT handler)。保留 executeApproval 的 try/catch + console.error 真实错误处理
4. server.js / index.html 真实 bug 修复（saveRecordAsync resp.ok 检查、executeApproval try/catch）保留
5. locales 补充 status i18n key: completed="Completed"、level3Approved="Level 3 Approved"

**git 流程**:
- commit `fd2984d` on main: "v3.9.14: 修复列表审批状态列为空 - 改用原始状态兜底(Pending Approval)，移除调试日志与横幅，补全 status i18n"
- `git push origin main` 成功 (95b545c..fd2984d)
- Render 自动部署成功，已 curl 验证线上为 v3.9.14 + 含 Pending Approval 兜底 + 无 DEBUG 残留

**结论**: 列表审批状态列空白根因 = 用户此前一直访问 Render 生产环境，而本地调试改动未部署；并非代码逻辑错误（兜底 `|| '待审批'` 若部署必生效）。

## 线上修复：进入系统后概览页空白 (v3.9.15 / v3.9.16)

**现象**: 登录进入后默认"售后概览"侧栏选中、标题正确，但主内容区域完全空白；Network 接口有返回。

**根因（两个叠加）**:
1. `enterApp()` 只调用 `handleHashRoute()`；用户 URL 是 `?lang=en-US` query 参数（**无 hash**），`handleHashRoute` 在 `if(!hash) return` 直接退出 → 没有任何 `showPage` 被调用 → `page-overview` 从未激活、概览从未渲染。
2. **更隐蔽的真凶**：`renderOverview` 先填好 `ov-summary`（第2639行），但随后 `new Chart(...)`（第2653行起）抛异常（Chart.js 从 `cdn.jsdelivr.net` 加载，生产/国内网络常被墙 → `Chart` 未定义）→ 被 `catch` 捕获 → **清空 ov-summary 与 ov-risk-row**（原2662-2663行）→ 整页空白。即"接口有数据，但图表异常把页面清空了"。

**修复 (index.html)**:
- `enterApp()` 末尾加：`if(!document.querySelector('.page.active')){ showPage('overview'); }` → 无 hash 时默认进入概览
- 重写 `renderOverview`：**数据/卡片/最近记录渲染 与 图表渲染彻底解耦**。主内容用独立 try 渲染（异常只显示空状态，不再清空）；图表用新增 `safeRenderChart()` 包裹（`typeof Chart==='undefined'` 直接跳过 + 各自 try/catch），图表失败绝不影响主内容
- 新增 `ovCard`/`ovCardClick`/`safeRenderChart` 辅助函数
- 概览看板新增兜底内容：今日售后数 / 待审批 / 待处理 / 已完成（卡片）+ 最近售后记录列表（`ov-recent` 容器，空数据显示"暂无数据"）
- 补充 i18n key：`overview.card.today/completed`、`overview.recent.title/empty/pending`（en-US/zh-CN/id-ID）

**验证方式（无浏览器也能验）**: Node + vm 沙箱加载内联脚本并真实执行 `renderOverview`（桩 DOM + fetch 返回样例 + Chart 故意不定义）→ 确认不抛错、TEST 标记/卡片/空状态均渲染，且 Chart 缺失时主内容未被清空。

**部署**:
- commit `903136c` (v3.9.15) 含 TEST 标记，部署后 curl 验证线上=TEST 标记存在、v3.9.15
- commit `5a73105` (v3.9.16) 移除 TEST，部署后 curl 验证线上=v3.9.16、TEST 已移除、兜底 key 与路由修复均在
- 两 commit 均 `git push origin main` 触发 Render 自动部署成功

**经验**: 生产环境加载第三方 CDN（Chart.js/jsdelivr）可能在国内被墙，凡依赖外部 CDN 的组件必须把失败隔离在局部 try/catch 内，绝不能让异常回滚已渲染的主内容。

---

## 店铺/客户显示名映射修复 (v3.9.17)

**问题**: 售后列表/详情/审批页的"店铺/客户"列直接显示了内部编码（如 `shopee_netac`、`tiktok_netac`），而非字典管理里的可读名称（Shopee Netac、Tiktok Netac）。字典表 `dictionaries`(category=`shop_customer`) 的 `code` 即存储值，`label_zh/en/id` 为显示名。

**根因**: `shop_customer` 存储值本就是 `code`（内部编码），前端多处直接 `esc(it.shop_customer)` 输出，未做 code→label 映射。且 `_dictCache['shop_customer']` 只在打开"字典管理"页时填充，列表/详情/审批渲染时该缓存为空。

**修复 (index.html)**:
- 新增 `ensureShopCustomerDict()`（经 `loadDictOptions('shop_customer')` 全量预加载，结果缓存在 `_shopCustomerDict`，失败兜底为 `[]`）+ `getShopCustomerLabel(value)`：按 code 精确 / code 不区分大小写 / label 匹配，命中返回 `getDictLabel`(按当前语言)；均不命中兜底返回原始值（避免空白）。
- 在 `renderDetail`、`renderList`、`openLegacyApprovalModal` 渲染前 `await ensureShopCustomerDict()`。
- 替换显示点：`getShopCustomerLabel(it.shop_customer)` 用于 列表-明细模式(2939)、详情页(3288)、审批弹窗摘要(4587)；列表-按单模式聚合(2961) 改为先 map 成 label 再 join。
- **原始数据未改**（提交/编辑仍存 `code`；导入 normalizeShopCustomer 不变）。

**有意未改**: CSV 导出(5463/5492)仍输出原始 `code`，保留数据口径与可再导入性（如需要可一并映射）。

**验证**:
- `node --check` 内联脚本语法 OK。
- 映射单测全通过：`shopee_netac→Shopee Netac`、`tiktok_netac→Tiktok Netac`、大写 `SHOPEE_NETAC` 兼容、未命中兜底原值、空值→空。
- 部署：commit `ec155d8`(功能) + `19db576`(标题版本号同步) 均 `git push origin main` 触发 Render。线上 curl 确认 `getShopCustomerLabel` 已上线、版本 v3.9.17。

---

## 渠道(平台)显示名映射 + 通用字典函数 (v3.9.18)

**问题**: 售后详情/列表/审批页"渠道"列直接显示原始 code（`shopee`/`tiktok`），未映射字典显示名（Shopee/TikTok）。根因：`getPlatformLabel` 依赖 `_platformCache`，但该缓存只在"新建/编辑表单"(openRecordForm/renderDetail编辑模式)加载；**查看模式**渲染详情/列表/审批时 `_platformCache` 为空 → 回退显示原始 code。

**重构（用户要求抽通用函数）**:
- 新增通用 `getDictionaryLabel(category, value, fallbackList)` + `_dictLabelCache`(按分类缓存) + `ensureDictLabelCache(category)`(经 `loadDictOptions(category)` 预加载，失败兜底 `[]`)。匹配优先级：code 精确 / code 不区分大小写 / label 匹配 / 兜底原值。
- `getPlatformLabel(code, platformList)` → `getDictionaryLabel('platform', code, platformList || _platformCache)`。
- `getShopCustomerLabel(value)` → `getDictionaryLabel('shop_customer', value)`；`ensureShopCustomerDict()` → `ensureDictLabelCache('shop_customer')`（移除旧 `_shopCustomerDict`）。
- 以后 退货原因/退货方式 等都可统一走 `getDictionaryLabel`（RETURN_REASON 当前走 i18n map，暂未迁移）。

**修复点**:
- `renderDetail`/`renderList`/`openLegacyApprovalModal` 已有 `await ensureShopCustomerDict();` → 追加 `await ensureDictLabelCache('platform');`（replace_all）。
- `renderApproval` 无该调用 → 单独加 `await ensureDictLabelCache('platform')`。
- 列表-按单模式(2975)与审批页(4520)的 `platforms` 聚合数组改为 `map(it=>getPlatformLabel(it.platform))`，记录级兜底 `getPlatformLabel(r.platform)`。

**有意未改**: CSV 导出(平台5462/5491、店铺5463/5492)仍输出原始 code（数据口径/可再导入）。表单下拉(3472/3481)正确（value=code, label=显示名，编辑用）。

**验证**: `node --check` OK；通用映射单测全通过（platform: shopee→Shopee, tiktok→TikTok, 大写/显示名兼容, 未命中兜底, 空→空；shop_customer 同前）。部署 commit `f1e588a` → `git push origin main` 触发 Render，线上 v3.9.18。

---

## Approval Status 多语言统一 (v3.9.19)
**问题**: 切换英文/印尼语后 RMA Cases 列表/概览的 Approval Status 仍显示中文（如"已完成"）。
**根因**: 列表两模式(2935/2972)和概览最近记录(2654/2658)直接输出 `r.status` 原始中文，绕过了 `tApprovalStatus()`；且旧 `tApprovalStatus(zh)` 只认中文并映射到 9 档细分状态，不是用户要的 3 态统一模型。
**改动**:
- 重写 `tApprovalStatus`（index.html ~1233）：`APPROVAL_STATUS_CANON` 把中文/英文/旧值归一化为 completed/pending/rejected 三档，`APPROVAL_STATUS_LABEL_KEY` 映射到 `approvalStatusDisplay.*` i18n key；未知值兜底返回原值（不空白）。
  - 映射: 已完成/completed/approved/审批通过/各级通过→completed；待审批/pending/pending_approval/待X级审批/草稿→pending；已拒绝/rejected/审批拒绝/已驳回→rejected。
- 三语言新增 `approvalStatusDisplay` 命名空间：en(Completed/Pending Approval/Rejected)、zh(已完成/待审批/已拒绝)、id(Selesai/Menunggu Persetujuan/Ditolak)。
- 列表两模式(2935/2972)与概览(2654/2658)改用 `tApprovalStatus(...)`；详情(3278)/审批列表(4517)/审批详情(4590)因已调用该函数自动受益。
- 原始数据未改动。
**验证**: `node --check` + JSON 校验 OK；17 例映射单测全通过（含三语言、大写/英文兼容、未知兜底、空值）。部署 commit `51cc4a6` → `git push origin main` 触发 Render，线上 v3.9.19。


---

## 审批通过自动「已处理」治本+治标 (v3.9.20)
问题: 可二次销售/ERP入库明细审批通过后仍是「待处理」。根因：原 executeApproval 的自动 completed 被 if(returnDate) 卡住，审批通过但未填退货日期时明细不更新；#4 即此类旧数据（4 条 erp/可二次销售 仍 pending）。
治本 (index.html executeApproval ~4420): 删除 if(returnDate) 门槛；审批通过(final)时遍历 items，凡 process_type=erp/ERP入库 或 return_reason=可二次销售/resellable 且 progress!=completed -> 置 process_progress=completed / process_status=已处理 / process_completed_date=returnDate||now；填了 returnDate 才写 return_stockin_date。不动 待换彩盒(color_box)/待补配件(parts)/待RMA(rma)/待报废(scrap)。
UI 放松 (doApproval ~4681): 删除 willFinishApproval&&pendingErpQty>0&&!returnDate 的强制拦截，returnDate 仅可选补充。
治标脚本 scripts/fix_resellable_pending.js: 连接生产库(./db)，SELECT status IN(已完成/审批通过/completed) 记录，对符合条件且 pending 的明细置 completed/已处理/process_completed_date，UPDATE items(->jsonb 转换)。带 --dry-run。已对 #4 实跑：扫描1记录/修复1记录/4明细全部 completed，return_stockin_date 空。
展示层: 详情(3306)/审批弹窗(4615)/列表均 renderProcessProgressBadge(itemProcessProgress(it))，数据变 completed 即显示「已处理」，无需改展示代码。
验证: node --check OK；11 例匹配单测全过（正向 completed、4 类其他处理类型不误伤、已 completed 不动、returnDate 有/无正确）。部署 commit d959bfb -> git push origin main 触发 Render，线上 v3.9.20。


---

## 售后概览统计卡片标题空白修复 (v3.9.21)
问题: 概览页第2张(待审批)、第3张(待处理)卡片只显示 icon+数字、无标题；第1(今日售后)/4(已完成)及风险卡(超期未处理/待RMA)正常。
根因: 渲染代码 renderOverview (~2646-2647) 用 t('overview.card.pendingApproval') / t('overview.card.pendingProcess')，但这两个 key 仅存在于 overview.risk 命名空间，overview.card 下缺少 -> t() 对缺失 key 返回空串(locales/i18n.js 第74行 return value||'') -> 空标题 div。
修复: 在 overview.card 命名空间补齐 pendingApproval/pendingProcess 三语言：
  zh-CN: 待审批/待处理；en-US: Pending Approval/Pending Processing；id-ID: Menunggu Persetujuan/Menunggu Diproses。
  (与现有 overview.risk 同义；id 的 pendingProcess 用用户指定的 Menunggu Diproses，未动 risk)。
未改统计数字逻辑与展示结构。
验证: 三文件 JSON 合法 + 6卡 key 三语言均解析正确；index.html 未改动 node --check OK。部署 commit 6f59482 -> git push origin main 触发 Render，线上 v3.9.21。


---

## Favicon 专属图标替换 (v3.9.22)
问题: 浏览器 Tab 显示默认灰色地球图标，无品牌辨识度。
根因: index.html <head> 中无任何 favicon <link> 引用，且项目根目录无 favicon.ico/favicon.png 等文件。
修复:
- 用 sharp(已安装于受管 node workspace) 栅格化 SVG: 绿色圆角方形(#2e7d32 匹配 --primary) + 白色剪贴板/单据图形 + 绿色勾选标识。
- 生成 4 个文件: favicon.ico(PNG-in-ICO,256x256), favicon.png(512x512), apple-touch-icon.png(180x180), manifest.json(PWA 图标引用)。
- index.html <head> 新增: theme-color(#2e7d32) + link rel=icon(favicon.png/ico) + apple-touch-icon + manifest，全部带 ?v=2 破浏览器缓存。
- server.js 已有 express.static(__dirname) 托管根目录静态资源，无需改后端。
验证: JS 语法 OK; manifest JSON 合法; file 命令确认 ICO/PNG 格式正确。部署 commit 20d0c68 -> git push origin main 触发 Render，线上 v3.9.22。

---

## 售后系统前端结构/业务逻辑梳理（只读分析，未改代码）
用户要求先不修改代码，把当前系统梳理成结构化文档，方便交给 ChatGPT 做 UI 重构方案。
已产出 `AFTERSALES_UI_REFACTOR_BRIEF.md`（项目根目录），按用户指定十节格式：技术栈 / 页面清单 / 核心业务流程 / Overview 逻辑 / 列表字段操作 / 新建表单 / 详情页 / 系统管理页 / 不可改动业务逻辑红线 / 建议重构文件。
核心结论：
- 前端是 index.html 单文件 SPA（7450 行内联 JS/CSS），无框架/无 UI 库/无路由库，Chart.js 走 CDN。
- 侧边栏 renderSidebar(:2481) 按权限动态生成；路由 showPage(:2524) 切换 .page 显隐。
- 11 个 DOM 页：login/overview/list/detail(Modal跳转)/approval/products/users/roles/customers/dictionaries/approval-flows；"新建售后记录"是 record-form-modal 而非独立页。
- 审批状态六态、处理状态三态(pending/processing/completed)、处理类型5种(erp/color_box/parts/rma/scrap，由 return_reason 经 RETURN_REASON_TO_PROCESS_TYPE 映射)。
- ERP 自动 completed 治本逻辑在 executeApproval(:4427)；超时=待处理且≥7天(isOverdueItem :2602)。
- 重构红线：飞书登录/客户Webhook同步/审批流状态机/导入导出契约/状态计算/权限控制/多语言/DB schema 不可改。
下一步（用户待办）：可能据此让 ChatGPT 出重构方案，或让我执行拆分改造（届时需保持上述红线与 server.js 接口契约不变）。

---

## UI 重构 Phase 1：Apple 风格全局主题 + Layout/Sidebar/Header + 基础组件 (v3.10.0)
用户启动第一轮 UI 重构（基于 AFTERSALES_UI_REFACTOR_BRIEF.md）。原则：只做视觉/布局，不动架构/后端/DB/状态机/导入导出/状态计算/权限/多语言 key。分四阶段提交，每阶段部署后等用户截图确认再继续。

**Phase 1 范围（已完成并部署 commit 32f135f，线上 v3.10.0）**：
- `:root` CSS 变量改为 Apple 调色板：`--primary:#0071e3`(蓝)、`--sidebar-bg:#1d1d1f`(低饱和深色磨砂)、`--bg:#f5f5f7`、卡片圆角 `--radius-card:16px`/`--radius:14px`、按钮 `--radius-btn:10px`、pill `999px`、阴影变轻（`--shadow`/`--shadow-modal`）；补 `--bg-secondary/--bg-alt/--hover-bg/--primary-dark/--border-light`（原未定义导致透明）。
- Layout：`.app-shell` 浅灰底；`.sidebar` 宽 184→220（与 record-fs-overlay left:220 对齐）、深色磨砂、选中态改柔和高亮 `rgba(255,255,255,.10)` 不再深绿；`.topbar` 改 macOS 工具栏（半透明白+backdrop-filter+0.5px 细分隔线）。
- 基础组件：按钮圆角 10px；表格行高加大(14px padding)、分割线变浅(`--border-light`)、hover 中性浅灰；模态圆角 16px + 轻遮罩 + 新增 `.modal-footer` 基础样式；状态/角色/用户/产品等标签统一 pill 圆角。
- Sidebar 导航 emoji → 统一线性 SVG 图标（renderSidebar 内 ICON map，Feather 风格）；品牌图标改剪贴板 SVG；语言切换 select 改 `.lang-select` 类。
- **修复导航选中态丢失**：`renderSidebar` 重建前记录当前 active 页、重建后 `setSidebarActive(prevActive)` 恢复（原函数用 innerHTML= 替换不会真重复，但重建会清空选中态 → 这是用户反馈"导航重复/选中异常"的根因）。
- 新增基础组件 CSS 储备（供 Phase 3/4 用）：`.more-btn` + `.dropdown-menu`/`.dropdown-item`（统一"更多操作"菜单）、`.line-icon` helper、`.card` 通用卡片。
- 版本：title suffix 与 package.json 3.9.21→3.10.0；sidebar-footer 陈旧 "v3.9.3"→"v3.10.0"。

**有意未改（属后续阶段/红线）**：Overview 重排(Phase2)、列表/新建弹窗/详情(Phase3)、管理页统一样式(Phase4)；登录页绿渐变(飞书登录红线)、各页 emoji(概览卡/空态/审批流图标)留待对应阶段；业务状态机/导入导出/权限/多语言 key 全部未动。

**验证**：`node --check` 内联脚本 OK；CSS 括号 481/481 平衡；Render 部署后 curl 确认线上 v3.10.0 + 静态 HTML 含新样式。

**待办**：等用户线上截图确认 Phase 1 视觉，再启动 Phase 2（Overview 系统性重排）。

## UI 重构 v3.10.1–v3.10.2（Phase 1 补 + Phase 2 + 细节优化）

**v3.10.1 紧急修复（详情页崩溃）**：
- 根因：`getProductsAsync` 缓存 `resp.json()` 前未校验数组，接口异常/空响应会污染 `_productsCache`，导致详情页 `productBySku=new Map(products.map(...))` 抛 `products.map is not a function`。
- 修复：缓存前规范化响应为数组（Array / data.data / rows / 兜底 []），保护所有消费者。不动业务/接口/状态机。
- commit e82dec2，线上 v3.10.1。

**v3.10.2 Overview 系统性重排 + UI 细节**：
- Overview 按确认方案重排：筛选区、After-sales Health(Good/Attention/Critical+总数+完成率+超期+瓶颈)、RMA Flow Overview 横向 5 节点(数量/占比/Avg stay/Overdue)、趋势(30天 Created/Completed/Overdue)+Stage donut、异常分析(Top Return Reasons/Top SKU)、Need Your Attention 行动区(超期/待审批>24h/待处理>3天/RMA未创建>7天)+Recent。复用现有 isPendingApproval/isOverdueItem/isPendingRmaItem 等计算，不新增业务状态。
- 三语言补 overview.health/flow/attention/dataAsOf/chart key；list.action.* 补 more。
- 主色改柔和绿 #2e9d52（去强蓝）。
- 操作列统一「更多操作」下拉菜单（moreMenuHtml/toggleDropdown/closeDropdowns，全局 ICON_ACTION 线性图标），列表(按单/按明细/审批页)+商品/用户/角色管理页 emoji👁✏🗑🔔 全部替换为线性图标；列表标题 📝 换文档线性图标。
- 表格横向滚动条轻量化（::-webkit-scrollbar 8px 浅灰）。
- 新建记录弹窗按钮已纯文本(data-i18n)，无需改。
- commit a58798a，线上 v3.10.2。
- 红线全程未动：飞书登录、客户 Webhook、审批流状态机、ERP 自动 completed、RMA 判定、超期计算、CSV 导入导出、权限、多语言 key 结构、DB schema、server.js 返回结构。

## UI 重构 v3.10.3–v3.10.4（P0 菜单修复 + Round 2 精修）

**v3.10.3 P0 操作菜单被裁切修复**：
- 根因：.dropdown-menu 内嵌 .more-wrap(position:relative)，被祖先 .table-container(overflow-x:auto) / .table-section(overflow:hidden) 裁切。
- 修复：moreMenuHtml 仅输出按钮并登记菜单内容到 _dropdownRegistry；toggleDropdown 创建 #floating-dropdown 挂 body，position:fixed 按按钮 getBoundingClientRect 定位，自动向上翻转/向左对齐，横向滚动(scroll capture)时跟随重定位，按钮滚动出视口自动关闭；点击外部/菜单项关闭；z-index=150(高于表格/卡片/header，低于 modal 200)。覆盖 RMA Cases/按单/按明细/审批页所有 more 菜单。不改业务。
- 同时统一表格横向滚动条为 6px 浅灰(transparent track)，并复用于 .ov-flow/.af-flow-container。
- commit cb56f41，线上 v3.10.3。

**v3.10.4 Round 2 精修（仅视觉/布局，业务未动）**：
- Overview：After-sales Health 改为左侧独立状态卡片(Good/Attention/Critical 浅色块+线性图标)+右侧4指标横向卡；瓶颈文案去强橙改柔和提示色；RMA Flow 节点改独立小卡片(柔和色系 #5cb87f/#e0a13c/#5b9bd5/#a987c9/#9aa0a6 + 10%~15% 透明底)+更轻箭头；图表网格线更浅、标题/图例间距加大、donut 调小(240)、trend 加宽(264)；异常分析柱状图改柔和绿 #5cb87f；Need Your Attention 改行动清单(左侧线性图标+数量+箭头，count=0 整行灰色 muted，标题加总待办 badge)；Recent 改精致网格列表+状态 pill。
- 详情页：顶部 6 张横向摘要卡(RMA Case#/Approval/Processing/SubmittedBy/TotalQty/PendingERP)；RMA Items 表头浅灰(#fbfbfd)+行高增加+长字段 cell-wrap；Approval Flow 改横向步骤条卡片(Initiator/Level1-3，含人员+状态 done/current/pending/rejected/empty 线性图标)；Approval History timeline 卡片更轻+统一 ah-badge 状态标签+备注降为次级文字；Time Info 改底部轻量卡片。
- 全局：--success 由高饱和 #34c759 降为柔和 #5cb87f；header 高度 56→54、边框 .08→.06；亮绿强调(af-step.done/ah-badge.approved)降饱和。
- 三语言补 detail.label.processingStatus/processingDone/processingPending/summary.rmaCase/step.*（仅新增 key）。
- commit 46a7d69，线上 v3.10.4。
- 红线全程未触碰（同 3.10.2 清单）。

## UI 精修 v3.10.5（Sidebar 去重 + Overview 微调 + 三页视觉统一）

**Sidebar 重复菜单排查结论**：经静态分析 + 拉取线上 HTML 比对，renderSidebar 为单函数、单一空 `<nav>`、9 个唯一菜单项、innerHTML 整体替换（幂等），代码层根本不存在重复渲染。用户截图看到的重复极可能是**旧构建的浏览器缓存**。仍做了防御性加固：groups 渲染时按 item.id 用 `seen` Set 去重，任何情况同一菜单项只渲染一次。版本号升 v3.10.5 + 重新部署，引导用户硬刷新（Cmd/Ctrl+Shift+R）清缓存。

**Overview 微调（仅 CSS/图表参数，业务未动）**：
- 顶部筛选区更紧凑：.ov-filter-card 加 padding 14/18、margin 18→14；筛选输入/按钮高 36、字号 13；.ov-data-asof 字号 11、margin-top 8。
- After-sales Health 五卡统一：状态卡 padding 18→16、metric 圆角 12→14，与状态卡圆角/内边距完全对齐（高度已由 align-items:stretch 等同）。
- RMA Flow 连接线更清晰：::after 改 2px 实线 #aeb0b6 居中跨间隙，新增 ::before 箭头(`) 指向下一节点。
- Top 5 Return Reasons 柱体变薄：dataset 加 maxBarThickness:18 + barPercentage .6 + categoryPercentage .7；.chart-bar 高 250→228。
- Recent RMA Records 改紧凑列表：grid `auto 92px auto 1fr`，行 padding 9/10、行间 1px 分隔线、状态 pill + Submitted By 右对齐省略号截断。
- Need Your Attention 保留不改。

**三页视觉统一（仅 token，业务未动）**：
- 新建记录表单弹窗：.approval-flow-card 圆角 8→12、.approval-flow-preview 6→12；明细表输入/选择框圆角 4→8；.form-group input/select/textarea 6→8。
- 详情页审批步骤条连接线同步改为更清晰的实线+箭头（与 Overview Flow 一致）。
- 全局输入圆角统一 6→8px（.search-input/.filter-select/.date-input），三页输入框共一套圆角语言。
- 列表页本身已符合 Apple 风（.filter-bar/.data-table/.moreMenuHtml/.status-badge），无需改动。

- commit（本地 main）+ 推送 Render 自动部署；线上已验证 v3.10.5、dedupe 与 flow connector 代码生效。
- 红线全程未触碰（同 3.10.2 清单）。

## Sidebar 重复菜单 + Overview 微调 (v3.10.6)

**Sidebar 重复根因结论**：静态分析 + 拉取线上 HTML 比对，renderSidebar 单一函数、单一空 `<nav>`、9 个唯一项、innerHTML 整体替换（幂等），3 个调用点（登录 2700/存角色 6793/切语言 7055）均用 innerHTML=。代码层根本不可能重复。James 账号看到的重复 = 旧构建浏览器缓存（用户持续未硬刷新，或标签页未重载）。

**加固（保险）**：renderSidebar 开头 `nav.innerHTML=''` 先清空；结尾对 `.sidebar-item` 按 `data-page` 做 DOM 级二次去重（重复者 remove）。即便任何未知来源追加重复，渲染后自清理。保留 4 个正常分组与权限/多语言逻辑。

**Overview 微调（仅 CSS/图表参数，业务未动）**：
- 卡片标题贴边修复：`.ov-section` 加 `padding:18px 20px`（标题距上 18px/左 20px）；`.ov-section-title` 加 `margin:0 0 14px`（标题-内容 14px）；`.ov-section-head .ov-section-title{margin:0}` 避免注意力卡标题与计数徽标间多余间距。所有 Overview 模块统一同一规则。
- 顶部筛选区更紧凑：`.ov-filter-card` padding 14→12、`.ov-data-asof` margin-top 8→6。
- Top 5 Return Reasons 柱体：maxBarThickness 18(过细)→46(中等)，barPercentage .6→.55；保留柔和绿 #5cb87f 等配色。
- 未重排 Overview 结构（Filter/Health/Flow/Trend+Donut/Reason+SKU/Attention+Recent 保持）。

- commit + 推送 Render；线上已验证 v3.10.6、seenPages 去重与 maxBarThickness:46 生效。
- 红线全程未触碰（同 3.10.2 清单）。

## UI 精修 v3.10.7 + v3.10.8（Overview 收尾，仅样式）
- v3.10.7（前次，部署被中断后轮询确认已上线）：① 启动期版本缓存爆破 `bootVersionBust()`——比对 `<title data-i18n-suffix>` 与 `/api/version`，不一致则 `location.replace('?_v=<ver>')`，专治飞书 webview/proxy 对 index.html 的 URL 缓存（server.js 已发 no-store 仍被中间层忽略）；② Top5 Return Reasons 柱宽 maxBarThickness 46→72（单分类自然，非大色块，保留柔和绿）。
- v3.10.8：After-sales Health 指标卡布局微调（不改计算逻辑）。`.ov-health-metric` 加 `display:flex;flex-direction:column;justify-content:center`（左对齐+垂直居中），padding 16→20；`.ov-health-status` padding 16→20 与指标卡统一。状态卡与四指标卡等高（align-items:stretch + grid），内容垂直居中节奏一致；Bottleneck 卡保持「Current Bottleneck 上 / No major bottleneck 下」结构不变。
- 三处版本号同步（title suffix + sidebar-footer + package.json）；boot 缓存爆破依赖 title suffix，故每次升级必须同步改 title。
- 红线全程未触碰（同 3.10.2 清单）。下一步：进入 RMA Cases 列表页优化（用户已确认 Overview 主体通过，不再大改）。
