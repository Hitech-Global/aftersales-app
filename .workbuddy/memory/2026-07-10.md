# 2026-07-10 工作日志

## v3.11.7 — 日期选择弹窗(日历)被卡片/滚动容器裁切修复

背景：用户反馈 Create RMA Case 的 RMA Date 点击后,日期选择弹窗只露一部分,被 Basic Information 卡片或父容器裁切,无法选日期。

### 根因
`.custom-date-panel` 原为 `position:absolute`,且被 `enhanceDateInput` 追加到 `.date-picker-wrap` 内部;而 `.form-card{overflow:hidden}`(以及 modal body / 滚动容器)会裁切绝对定位后代 → 弹窗被切。

### 修复（纯 UI/定位,不改业务/审批/API/DB/权限/i18n）
- CSS：`.custom-date-panel` 改 `position:fixed;top:0;left:0;z-index:1000`(高于表单/Modal 遮罩 z-index:200,低于附件预览 9999 / 自动补全 99999)。
- JS：弹窗改 portal 到 `document.body`(`document.body.appendChild(panel)`),并 `input._datePanel=panel` 存引用;新增 `getDatePanel(input)` 替代原先 `input.parentElement.querySelector('.custom-date-panel')` 的查找(openCustomDatePanel / renderCustomDatePanel / setDateInputValue / applyDateInputLocale 全部改用)。
- 新增 `positionDatePanel(input,panel)`：基于 `getBoundingClientRect()` 用 fixed 定位,靠近底部自动向上展开、靠近右侧自动左对齐避免超出屏幕。
- 修正点击外部关闭逻辑：弹窗已不在 wrapper 内,需同时判断点击是否落在 panel 内,否则点选日期/翻月会被误关。
- 新增 scroll(useCapture 捕获任意滚动容器,含全屏表单 body) + resize 监听,打开期间跟随重定位。
- 影响所有 date input：Overview 筛选、RMA Cases 筛选、Create RMA Date、以及所有 Modal 内日期字段(appr-return-date / batch-process-date / batch-erp-date / detail-appr-return-date 等)。

### 验证
- 三 locale + package.json JSON 合法;node --check 内联脚本语法 OK。
- Render 部署后线上 curl 确认：v3.11.7、`.custom-date-panel{position:fixed...z-index:1000}`、`appendChild(panel)` / `positionDatePanel` / `repositionOpenDatePanel` / `_datePanel=panel` 均生效。

### 红线未触碰
业务逻辑 / 审批流程 / 审批人匹配 / 附件上传 / 保存草稿 / 提交审批 / 权限 / API / DB schema / 多语言 key 结构 / 日期格式 / 筛选逻辑 / 保存逻辑均未改动。

## v3.11.6 — Create RMA Case 页 RMA Items 修复 + 日期图标 + Basic 卡片收紧

背景：用户反馈 v3.11.5 的 RMA Items 明细区"被改坏"——字段被挤出/不可见（只到 Return Reason），Return Reason 显示 `Functio...`，日期图标跑到输入框外很远。

### 根因
1. RMA Items 表格用 `width:max-content; table-layout:fixed`——fixed 布局下 max-content 在部分场景不可靠，容器窄于列宽合计时仍被压缩，字段截断/不可见。
2. 日期图标：`.date-picker-wrap` 缺少 `position:relative`，内部绝对定位的日历按钮锚定到最近的定位祖先 = 全屏浮层 `.record-fs-overlay`（position:fixed），因此按钮被推到最右侧边缘。

### 修复（均不改业务/审批/API/DB/权限红线）
- 表格改为 `width:100%; min-width:1760px; table-layout:fixed`（用户明确要求的 min-width 方案）：列宽保底 1760px，容器更窄时由 `.detail-items-table-wrapper{overflow-x:auto}` 横向滚动，绝不压缩。
- 表头列宽按新规格：Channel120 / Shop200 / SKU240 / SN160 / Order180 / Qty80 / Return Reason220 / Issue Description320 / Attachments150 / Actions80（合计 1750px）。
- Actions 列 td 加 `padding-right:16px`，删除按钮不贴边；`position:sticky;right:0` 保持滚动时始终可见、不被挤出。
- Return Reason 列 220px + 输入框 `width:100%`，"Functional Issue" 完整显示（标准英文已在 v3.11.5 落地）。
- 日期图标：补 `.date-picker-wrap{position:relative;display:block;width:100%}`（合并了原 display:flex 的旧规则，去掉重复）；Basic 卡片内 `.form-card--basic .date-picker-wrap{display:inline-block;width:240px}` 约束包裹层宽度，使图标落在输入框内部右侧。
- Basic Information 卡片收紧：`.form-card--basic` 头部 padding 12→10 18px、内容 14→12 18px，减少垂直留白；RMA Date 输入框宽约 240px（不横跨整行）。
- 版本三处同步（title / sidebar-footer / package.json → 3.11.6）。

### 验证
- 三 locale + package.json JSON 合法；node --check 内联脚本语法 OK。
- Render 部署后线上 curl 确认：v3.11.6、`min-width:1760px`、列宽（sku240/reason220/actions80）、`date-picker-wrap{position:relative...}` 均生效。

### 红线未触碰
业务逻辑 / 审批流程 / 审批人自动匹配 / RMA Items 数据结构 / 附件上传 / 保存草稿 / 提交审批 / 权限 / API / DB schema / 多语言 key 结构均未改动。
